auto-install-peers=true
ignore-scripts=false
# pnpm v10 bloquea builds nativos no aprobados y por defecto verifica deps antes de cada script,
# lo que hacía fallar (exit 1) typecheck/test/dev. Desactivamos esa verificación previa.
verify-deps-before-run=false
